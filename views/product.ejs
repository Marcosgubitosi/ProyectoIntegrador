<%- include('./partials/head') %>
<body>
	<!-- Header -->
	<% if (locals.user != undefined) { %>
        <%- include('./partials/headerLogueado') %>
    <% } else { %>
        <%- include('./partials/header') %>
    <% } %> 	
	<!-- /Header -->
	
	<!-- product detail -->
	<div class="container products-wrapper">
		<div class="d-flex align-items-center">
			<h2 class="products-title"><%= info.nombre_producto %></h2> 
			<p class="product-detail-description">
				<a href="../product/product-add">Editar</a>
			</p>
		</div>
		<div class="product-detail">
			<div class="row">
				<article class="col-12 col-lg-8 product-detail-info">
					<img src='<%= info.nombre_archivo_producto %>' alt="" class="product-detail-img">
					<p class="product-detail-description">
						<%= info.descripcion_producto %></p>
				</article>
				<div class="col-12 col-lg-4">
					<section class="comments">
						<%  for( let i = 0; i < info.comentario.length; i++ ) { %>
							<article>
								<!-- <img src="%= info.comentario[i].foto_perfil %>" alt=""> -->
								<div class="data">
									<p class="user">
									<!-- <strong> <a href="../profile">< info.comentario[i].email %>:</a> </strong> -->
									<%= info.comentario[i].comentario %></p>
								</div>
							</article>
							<% } %>
							
						
					</section>
					<!--Evitamos que se rompa al mandarlo a una ruta-->
					<form class="form" action="../product" method="GET">
						<input class="form-input" type="text" value="" placeholder="Agregar comentario">
						<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
					</form>
					<hr>
				</div>
			</div>
		</div>
	</div>
	<!-- /product detail -->

	<%- include('./partials/footer') %>

</body>	
</html>